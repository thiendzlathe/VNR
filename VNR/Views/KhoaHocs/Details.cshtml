@model VNR.Models.KhoaHoc

@{
    ViewData["Title"] = "Details";
    var getlist = ViewBag.ListMonHoc as IEnumerable<VNR.Models.MonHoc>;
}

<h1>Details</h1>

<div>
<div>
    <h4>Subject</h4>
    <hr />
@*    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.TenKhoaHoc)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.TenKhoaHoc)
        </dd>
        <dd class ="col-sm-10">
            @Html.DisplayFor(model => model.TenKhoaHoc)
        </dd>
    </dl>*@
    <table class="table">
        <thead>
        </thead>
        <tbody>
            @foreach (var item in getlist) 
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.TenMonHoc)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.MoTa)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.KhoaHoc.Id)
                    </td>
                    <td>
                        <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                        <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                        <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
<div>

    <a asp-action="Index">Back to List</a>
</div>
